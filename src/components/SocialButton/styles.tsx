import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    borderStyle: 'solid',
    borderColor: '#CCD4DA',
    marginBottom: 20,
  },
  buttonContentStyle: {
    height: 50,
  },
});

export default styles;
